# 复制为 .env 后填写（勿提交 .env）
# copy .env.example .env

# ---------- DeepSeek（当前默认）----------
# 申请 Key：https://platform.deepseek.com
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-v4-flash
# 更强但更贵：deepseek-v4-pro

LLM_TIMEOUT=60

# 无 Key 时默认 mock；有 Key 仍想强制 mock 可设：
# LLM_MOCK=1
