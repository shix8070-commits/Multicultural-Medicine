# shared/  【负责人：张鸣】

全组共用的约定。张璨、何权 **只引用、不改字段名**。要加字段先在组里说，由张鸣改这一层。

| 文件 | 用途 |
| --- | --- |
| `contracts.js` | 函数名与返回值形状（检索 / 问答 / 信号 / 发帖） |
| `storage.js` | localStorage 键名，避免三人互相覆盖 |
| `state.js` | 页面级状态：当前 Tab、当前页 |

禁止：在这里写文献条目、写 Agent 提示词、写论坛帖内容。
